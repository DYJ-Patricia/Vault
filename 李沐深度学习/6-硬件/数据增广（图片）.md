[[卷积神经网络Convolutional Neural Network|CNN 具有平移不变性，但对缩放旋转不具备不变变性，故可用data augmentation]]

- 旋转、翻转、切割
# 切割

- 从图片中切割一块，然后变形到固定形状
    - 随机高宽比 (e.g. [3/4, 4/3])
    - 随机大小 (e.g. [8%, 100%])
    - 随机位置

# 颜色

- 改变色调，饱和度，明亮度 (e.g. [0.5, 1.5])

# 总结

- 数据增广通过变形数据来获取多样性，从而使得模型泛化性能更好
- 常见图片增广包括翻转、切割、变色

