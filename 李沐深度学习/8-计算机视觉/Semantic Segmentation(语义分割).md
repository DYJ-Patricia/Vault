
- 有label的有监督学习
- 语义分割将图片中的每个像素分类到对应的类别

# 应用

## 背景虚化

## 路面分割

# VS 实例分割(instance segmentation)

- 语义分割：分为狗、猫、人
- 实例分割：分为狗1、狗2、猫、人

# 转置卷积

- 卷积不会增大输入的高宽，通常要么不变、要么减半
- 转置卷积则可以用来增大输入高宽

  Y[ i : i + h , j : j + w ] + = X [ i , j ] ⋅ K
  
  ![[Pasted image 20260307234454.png]]

- 如图，相当于卷积操作的逆过程

## 为什么称之为 “转置”

- 是形状上的一种逆运算
- 对于卷积 Y=X⋆W（星号是转置的意思）
    
    - 可以对 W 构造一个 V，使得卷积等价于矩阵乘法 Y′=VX′
    - 这里 Y′,X′ 是 Y,X 对应的向量版本(==就是拉成一条==)
    
- 转置卷积则等价于 Y′=V^TX′
- 如果卷积将输入从 (h,w) 变成了 (h′,w′)
    - 同样超参数的转置卷积则从 (h′,w′) 变成 (h,w)
- 转置卷积是一种卷积
    
    - 它将输入和核进行了重新排列
    
- 同卷积一般是做下采样不同，它通常用作上采样

![[Pasted image 20260307235225.png]]

# 转置卷积是一种卷积



![[Pasted image 20260308182544.png]]

- 对卷积来说加填充输出会变大
- 但对转置卷积来说填充输出会变小

![[Pasted image 20260308183230.png]]


![[Pasted image 20260308184001.png]]



![[Pasted image 20260308184812.png]]

# 同反卷积的关系

- 数学上的反卷积（deconvolution）是指卷积的逆运算
    
    - 如果 Y=conv(X,K)，那么 X=deconv(Y,K)
    
- 反卷积很少用在深度学习中
- 我们说的反卷积神经网络指用了转置卷积的神经网络


# 总结

- 转置卷积是一种变化了输入和核的卷积，来得到上采样的目的
- 不等同于数学上的反卷积操作